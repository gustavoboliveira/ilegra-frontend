const lib = require("./functions");

const artist = "Arctic Monkeys";
const playlist = "Top Brasil";
const track = "Californication";

lib.getTotalTracksInAlbuns(artist)
lib.getQuantityArtistFollowers(artist)
lib.getTotalPlaylistResearched(playlist)
lib.getTotalTrackInPlaylist(playlist)
lib.getTrackAndArtistsByResearch(track)
lib.getDurationTotalTracksResearched(track)
lib.getQuantityTracksResearched(track)